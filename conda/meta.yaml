from pathlib import Path

meta_yaml_content = """
package:
  name: otf-rbfe
  version: "0.1.0"

source:
  path: ..

build:
  noarch: python
  script: python -m pip install .

requirements:
  host:
    - python >=3.9
    - pip
    - pymbar ==4.0.3
    - alchemlyb ==2.3.1
    - scipy ==1.8.1
  run:
    - python >=3.9
    - pymbar ==4.0.3
    - alchemlyb ==2.3.1
    - scipy ==1.8.1

about:
  home: https://github.com/MGKurnikovaGroup/otf_general_public
  license: MIT
  license_family: MIT
  summary: "On-The-Fly Optimization for alchemical binding free energy simulations using AMBER"
"""

meta_path = Path("/mnt/data/meta.yaml")
meta_path.write_text(meta_yaml_content.strip())

meta_path.name
